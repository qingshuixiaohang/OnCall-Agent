## Agent 技能

### 问题追踪器

本仓库的 issue 与规格说明以 markdown 文件形式存放于 `.scratch/<feature>/` 目录下。详见 `docs/agents/issue-tracker.md`。

### 分流标签

五个标准分流角色使用默认标签字符串（`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`）。详见 `docs/agents/triage-labels.md`。

### 领域文档

单上下文模式：仓库根目录下存放一个 `CONTEXT.md`，ADR 存放于 `docs/adr/` 目录。详见 `docs/agents/domain.md`。